# The Yarn Monetization Sprint — 7-Day Plan (PC Ollama Delegation)

> **For Hermes:** Execute task-by-task with delegate_task on `custom:pc-ollama`
> (sonct988/gemma4-26b-a4b-it-q4km-256k:latest). Respect HARD SIZE LIMIT: one
> small task per child, file list inlined, no research-first children.

**Goal:** Wire The Yarn (203 pages, /home/Drevik/vault/Affiliate/site/) to all three
zero-traffic revenue streams: own-product cross-sell, email capture, affiliate
link completion — verified end-to-end.

**Architecture:** Pure static HTML site (no build system). All changes are
idempotent Python scripts operating on index.html files + git commits per task.
Site deploys via Cloudflare Pages from GitHub repo `Drevik/theyarn`
(local: /home/Drevik/vault/Affiliate/site). Every task ends with a git commit
and a verifiable artifact (grep counts, live URL checks).

**Constraints (from pc-ollama-delegation skill):**
- PC Ollama free window: 03:00–21:00 UTC. Subagent 600s hard timeout.
- Children get: exact file paths + inline snippets. No vault exploration.
- Drevik must review anything before deploy push — review queue rule.

---

## Day 1 — Product cross-sell boxes (own products, no external approval)

### Task 1.1: Build the box-inserter script (I write in-session, ~15 min)

**Objective:** Idempotent script that injects an "Origin Loom recommended
tools" box before `</article>` (or before `<footer` if no article tag) in
matching pages, with per-page product mapping.

**Files:**
- Create: `/home/Drevik/vault/Affiliate/scripts/add_origin_loom_boxes.py`
- Map file: `/home/Drevik/vault/Affiliate/scripts/origin_loom_map.json`

**Mapping (inlined — no research needed):**
```
finance/AI/passive-income/crypto/arbitrage/bookkeeping pages
  → devops-llm-prompt-library ($?): "automate this with ready-made LLM prompts"
ai-productivity-tools page → humanization-toolkit + pacing-pulse
affiliate-marketing page → youtube-gap-method + writing-prompt-pack
tokenized-real-estate / rwa-tokenization pages → skip (no matching product)
```
Box HTML template (uses Payhip originloom store URL pattern
`https://payhip.com/b/{slug}` — slugs filled after listings exist, box hidden
with `display:none` until then? NO — YAGNI: render box only when a real
Payhip URL exists in the map; script skips pages with null URLs).

**Step 1:** Write map JSON (page-dir → {product, slug_or_null, blurb})
**Step 2:** Write inserter (check `"origin-loom-box"` marker before writing → idempotent)
**Step 3:** Dry-run on 3 pages, diff check, then apply to all matches
**Step 4:** `grep -rc "origin-loom-box" site/*/index.html | grep -v :0 | wc -l` → expected ≈ 60
**Step 5:** git add + commit "feat: origin-loom cross-sell boxes"

### Task 1.2 (child): QA read of inserter output
**Objective:** Child diffs 5 random modified pages, verifies: valid HTML,
box appears exactly once, no nested-box corruption, rel="nofollow sponsored"
on external links only. Returns pass/fail + file list.

---

## Day 2 — Email capture infrastructure

### Task 2.1: Create lead magnet (delegate to PC Ollama: single-file task)
**Objective:** Build `gloomfang-teaser.html` landing page + 500-word
"welcome sequence" text from existing product `gloomfang-mine-collapse`
(source: /home/Drevik/vault/Gumroad/products/gloomfang-mine-collapse/).
Output: /home/Drevik/vault/Affiliate/site/assets/gloomfang-teaser.html
**Verify:** file exists, contains subscribe form stub.

### Task 2.2: Newsletter provider decision + signup form
**Objective:** Add footer email-capture to site template pages.
- Provider: Buttondown (free tier, API-friendly). Needs Drevik account signup
  → USER BLOCKER listed in final report.
- Create: `/home/Drevik/vault/Affiliate/scripts/add_email_forms.py`
- Injects one-line footer form (action=Buttondown URL placeholder env var
  `BUTTONDOWN_URL` read from ~/.hermes/secrets/buttondown.env).
- Idempotent via `email-capture` marker. Commit "feat: email capture forms".

### Task 2.3 (child): QA all pages still valid after form injection
Random sample 8 pages: form count == 1, no duplicate <footer>, HTML parses.

---

## Day 3 — Affiliate link completion (the 116 linkless pages)

### Task 3.1: Classify linkless pages (child batch QA task)
**Objective:** For each of the 116 pages (list generated by:
`grep -rL "tag=drevikbsg-20" site/*/index.html`), child reads title+first H2
and classifies: `product-adjacent` (a real Amazon product could be recommended)
| `tool-adjacent` (SaaS/tool → future CJ/ShareASale) | `pure-editorial` (leave).
Output: JSON classification to
`/home/Drevik/vault/Affiliate/scripts/linkless_classification.json`
Batch: 4 children × 29 pages each. Each child writes its own partial file
(linkless_classification_batch{n}.json) — merge in-session after.

### Task 3.2: Insert product boxes on product-adjacent pages
**Objective:** Reuse Day-1 inserter pattern with Amazon links
(tag=drevikbsg-20, rel="nofollow sponsored"). Product picks reviewed
by me in-session (gemma4-26b picks need adult supervision) before apply.
Verify: linkless count drops 116 → ≈60 (pure-editorial pages acceptable).

---

## Day 4 — Payhip store completion (USER CO-OP REQUIRED)
### Task 4.1: Script generates all 17 listing drafts from
`/home/Drevik/vault/Gumroad/products/*/` (title, price, description draft)
→ single HTML checklist file for Drevik to click through in Payhip UI.
### Task 4.2: Drevik creates listings in Payhip web UI (30 min, his side)
### Task 4.3: Once live: fill real slugs into origin_loom_map.json,
re-run Task 1.1 inserter, boxes go from hidden → visible. Commit.

---

## Day 5 — Verification + deploy

### Task 5.1: Full site QA (child batch)
- All 229 pages: python -c html.parser check (no exceptions)
- Every external link has rel="nofollow sponsored"
- No placeholder strings: `grep -r "PAYHIP_SLUG\|BUTTONDOWN_URL\|YOUR_ASSOCIATE_ID" site/` → 0 hits
### Task 5.2: Sitemap regenerate + commit + push to origin main
(CLOUDFLARE deploys automatically). Verify live:
`curl -s https://theyarnreview.com/best-yoga-mats/ | grep -c origin-loom-box`
### Task 5.3: Update earnings dashboard script: add "origin-loom-boxes live"
+ "email forms live" counts as new dashboard rows.

---

## Day 6-7 — Buffer + analytics verification
- Cloudflare Web Analytics: verify token present on live pages
  (curl | grep cloudflareinsights) — if Drevik enabled it Day 1 blockers.
- Wire GSC property check: curl for google-site-verification meta → confirm.
- Run earnings dashboard manually; confirm new rows render.
- Write week-2 plan only if Day-1-5 fully verified.

---

## User blockers to surface at each step
1. Day 2: Buttondown account (free) — needs signup, 5 min
2. Day 4: Payhip listings + Stripe connect — 30 min UI work
3. Day 5: git push approval (review queue rule — no auto-deploy)

## Success criteria (verifiable)
- [ ] ≥55 pages carry origin-loom box (hidden until Day 4)
- [ ] Every page has exactly 1 email capture form
- [ ] Linkless-product pages drop to ≈0, editorial pages documented as intentional
- [ ] 0 HTML parse errors across 229 pages
- [ ] All changes committed; push gated on Drevik review
- [ ] Earnings dashboard shows new monetization rows